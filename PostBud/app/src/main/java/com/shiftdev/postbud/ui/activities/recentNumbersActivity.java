package com.shiftdev.postbud.ui.activities;
import android.os.Bundle;
import com.shiftdev.postbud.R;
import androidx.appcompat.app.AppCompatActivity;


public class recentNumbersActivity extends AppCompatActivity {
    public void onCreate(Bundle savedInstanceState){
        super.onCreate(savedInstanceState);
        //set the user interface layout(recent_numbers.xml) for this activity
        setContentView(R.layout.recent_numbers);

    }
}
